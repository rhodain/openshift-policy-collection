apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  annotations:
    apps.open-cluster-management.io/ocm-managed-cluster: rhodain02
    apps.open-cluster-management.io/ocm-managed-cluster-app-namespace: openshift-gitops
    argocd.argoproj.io/refresh: normal
    argocd.argoproj.io/skip-reconcile: "true"
  labels:
    apps.open-cluster-management.io/pull-to-ocm-managed-cluster: "true"
    cluster: rhodain02
  name: rhodain02-troubleshooting
  namespace: openshift-gitops
spec:
  destination:
    name: rhodain02
    namespace: default
  project: default
  source:
    helm:
      releaseName: troubleshooting
    path: openshift-clusters/apps/troubleshooting
    repoURL: https://github.com/rhodain/openshift-policy-collection.git
    targetRevision: lpechacek/dev
  syncPolicy:
    automated:
      selfHeal: true
    syncOptions:
      - CreateNamespace=false
      - SkipDryRunOnMissingResource=true
      - Wait=true
